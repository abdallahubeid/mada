<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            The suite exhausts PHP's default 128MB before it finishes: ~780
            tests each rendering full Blade view trees accumulate component
            state faster than the limit allows, and it dies mid-run in
            ManagesComponents.

            This has to live here rather than being passed as `php -d
            memory_limit=...` on the command line, because Pest runs the tests
            in child processes that do not inherit the parent's -d flags —
            they re-read php.ini and get 128MB again.
        -->
        <ini name="memory_limit" value="1G"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="3306"/>
        <!--
            Pre-rebrand schema name, kept deliberately. See the comment above
            DB_DATABASE in .env.example: renaming the string without renaming
            the MySQL schema breaks the connection, and this name is never
            user-visible. If it is ever renamed, this entry and .env must move
            together — a green suite against `veyra_testing` proves nothing
            about an app pointed at `mada`.
        -->
        <env name="DB_DATABASE" value="veyra_testing"/>
        <env name="DB_USERNAME" value="root"/>
        <env name="DB_PASSWORD" value=""/>
        <env name="DB_URL" value=""/>
        <env name="PAGINATE_PAGE" value="10"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
